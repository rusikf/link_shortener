require 'spec_helper'

describe LinkShortener do
  it 'has a version number' do
    expect(LinkShortener::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
